plugins {
    id 'org.cat.eye.performance.test.java-application-conventions'
}

dependencies {
    implementation 'org.apache.commons:commons-text'
    implementation project(':test-scenario')
}

application {
    // Define the main class for the application.
    mainClass = 'org.cat.eye.performance.test.app.App'
}
